---
title: ValidPath
description: Type definition that returns the path if valid, otherwise the first possible valid path.
source: /packages/core/src/types/path/path.ts
contributors:
  - fabian-hiller
---

import { ApiList, Link, Property } from '~/components';
import { properties } from './properties';

# ValidPath

Conditional type that returns the path if valid, otherwise the first possible valid path based on the given value.

## Generics

- `TValue` <Property {...properties.TValue} />
- `TPath` <Property {...properties.TPath} />

## Definition

- `ValidPath` <Property {...properties.ValidPath} />

## Related

### Methods

<ApiList
  items={[
    { text: 'focus', href: '/methods/api/focus/' },
    { text: 'getErrors', href: '/methods/api/getErrors/' },
    { text: 'getInput', href: '/methods/api/getInput/' },
    { text: 'reset', href: '/methods/api/reset/' },
    { text: 'setErrors', href: '/methods/api/setErrors/' },
    { text: 'setInput', href: '/methods/api/setInput/' },
  ]}
/>
